import { View, Text } from 'react-native'
import React from 'react'

const Header = (
  ss
) => {
  return (
    <View style={{
      paddingTop: 64,
      textAlign: 'left',
      backgroundColor: 'red'
    }}>
      <Text>Header</Text>
    </View>
  )
}

export default Header